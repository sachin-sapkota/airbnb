name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Build and Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ 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 }}
        publish_dir: ./ # Change this if your website is in a subdirectory
